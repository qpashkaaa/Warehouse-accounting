<UserControl x:Class="Warehouse_accounting.View.Components.CustomTextBoxReadOnly"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Warehouse_accounting.View.Components"
             mc:Ignorable="d" x:Name="tb_UserControl">
    <Border BorderThickness="1"
            CornerRadius="8,8,8,8" BorderBrush="#EFF1F4"  Background="#F5F6F7" Width="446" Height="45" Margin="0 31 0 0">
        <Grid>
            <TextBlock x:Name="tbPlaceholder" FontSize="16" FontWeight="Bold" Text="{Binding ElementName=tb_UserControl, Path=CustomTextBoxText, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" HorizontalAlignment="Left" Panel.ZIndex="-1" Margin="8 0 0 0"/>
        </Grid>
    </Border>
</UserControl>
